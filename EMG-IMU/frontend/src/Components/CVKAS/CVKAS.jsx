import React from 'react';
import './CVKAS.css'

const CVKAS = ({ label, type = "text", name, placeholder, value, onChange }) => {
    return (
        <section className="cvkas-section">
            
        </section>
    )
}

export default CVKAS